<div class="container mb-5">  
    <div class="row featurette">
      <div class="col-md-5 m-auto p-4 text-center">
        <h2 class="featurette-heading"><%= about.title %> <br /><span class="text-muted lead"><%= about.subtitle %></span></h2>
        <p class="lead"><%= about.body %></p>
      </div>
      <div class="col-md-7 m-auto pt-5">
         <%# <%= image_tag "#{about.main_image}", class: "img-fluid" %>
         <%# <%= image_tag "banner-01-1024x450.png", class: "img-fluid" %> 
      </div>
    </div>
    <p class="pt-2">
            <% if user_signed_in? && current_user %> 
                <%= link_to 'Edit', edit_about_path(about) , class: "btn btn-secondary" %>
                <%= link_to 'Destroy', about, method: :delete, data: { confirm: 'Are you sure?' } , class: "btn btn-secondary" %>
            <% end %>
      </p>
</div>
    <%# <hr class="featurette-divider"> %>
    <%# <%= render "partials/about_image_gallery" %>
    <%= render "partials/more-gallery"%>
    <%# <hr class="featurette-divider"> %>
    <%# <h2 class="featurette-heading text-center">Where to find us</h2> %>
<%= render "partials/maps" %>